name: Shipping-CI-Main

on:
  push: 
    branches:
      - 'main'
  workflow_dispatch:
jobs:
   reuse:
    uses: warmup-b60/roboshop-github-actions/.github/workflows/ci-main.yml@main
    with:
      app_type: java